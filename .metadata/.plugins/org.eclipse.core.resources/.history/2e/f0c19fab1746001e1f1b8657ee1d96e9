package start.단순_2진_암호코드_1240;

import java.util.Arrays;
import java.util.Scanner;

public class Solution {

	public static void main(String[] args) {
		Scanner sc = new Scanner(System.in);
		
		int T = sc.nextInt();
		for (int t = 1; t <= T; t++) {
			int N = sc.nextInt(), M = sc.nextInt();
			int n = 0;
			char[] arr = new char[M];
			outer: while (n < N) {
				arr = sc.next().toCharArray();
				for (int m = 0; m < M; m++) {
					if (arr[m] == '1') break outer;
					
				}
			}
			int idx = M - 1;
			for (int m = 55; m < M; m++) {
				if (arr[m - 1] == '1' && arr[m] == '0') idx = m - 1;
			}
			idx -= 55;
			char[][] code = new char[8][7];
			for (int i = 0; i < 8; i++) {
				code[i] = Arrays.copyOfRange(arr, idx + 7 * i, idx + 7 * (i + 1));
				System.out.println(Arrays.toString(code[i]));
			}
			
		}
		
		sc.close();
	}

}
