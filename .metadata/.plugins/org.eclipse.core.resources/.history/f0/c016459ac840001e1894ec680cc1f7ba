package 요격_시스템;

import java.util.Arrays;

class Solution {
    public int main(int[][] targets) {
        int answer = 0;
        
        Arrays.sort(targets);
        System.out.println(Arrays.toString(targets));
        
        return answer;
    }
}